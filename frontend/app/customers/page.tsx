'use client'; import {useEffect,useState} from 'react'; import Nav from '@/components/Nav'; import {get} from '@/lib/api';
export default function Page(){const[d,setD]=useState<any[]>([]); useEffect(()=>{get('/customers?limit=20').then(setD).catch(()=>{})},[]); return <><Nav/><main className="ml-64 p-10"><h1 className="text-3xl font-semibold">Customers</h1><p className="muted mt-2">Synthetic customer records persisted through the FastAPI database layer.</p><div className="panel rounded-2xl mt-8 overflow-hidden"><table className="w-full text-sm"><thead className="bg-[#f0ece3] text-left"><tr><th className="p-4">Customer</th><th>Monthly income</th><th>Savings</th><th>KYC</th></tr></thead><tbody>{d.map(x=><tr className="border-t border-[#e5dfd2]" key={x.customer_id}><td className="p-4 font-medium">{x.customer_id}</td><td>₹{x.monthly_income.toLocaleString('en-IN')}</td><td>₹{x.savings_balance.toLocaleString('en-IN')}</td><td><span className="risk-low px-2 py-1 rounded-full text-xs">{x.kyc_status}</span></td></tr>)}</tbody></table></div></main></>}
