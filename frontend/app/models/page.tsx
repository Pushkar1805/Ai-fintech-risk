'use client'; import {useEffect,useState} from 'react'; import Nav from '@/components/Nav'; import {get} from '@/lib/api';
export default function Page(){const[d,setD]=useState<any>(null); useEffect(()=>{get('/model/metrics').then(setD).catch(()=>{})},[]); return <><Nav/><main className="ml-64 p-10 max-w-6xl"><h1 className="text-3xl font-semibold">Model performance</h1><p className="muted mt-2">Validation metrics from the synthetic training set.</p>{d&&<div className="grid md:grid-cols-2 gap-6 mt-8">{[['Credit',d.credit,d.selected.credit],['Fraud',d.fraud,d.selected.fraud]].map(([title,m,best]:any)=><div className="panel rounded-2xl p-6" key={title}><div className="flex justify-between"><h2 className="text-xl font-semibold">{title} risk</h2><span className="accent text-sm font-medium">Selected: {best}</span></div><div className="mt-5 space-y-3">{Object.entries(m).map(([name,v]:any)=><div className="flex justify-between border-b border-[#ebe5d9] pb-3" key={name}><span className="muted capitalize">{name.replaceAll('_',' ')}</span><span>{Number(v.roc_auc).toFixed(3)} AUC · {Number(v.f1).toFixed(3)} F1</span></div>)}</div></div>)}</div>}</main></>}
